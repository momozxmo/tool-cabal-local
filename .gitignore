# Build output (contains live user data — creds, Chrome profiles — never commit)
dist/
build/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Local runtime state (regenerated by the app; user/machine-specific)
.aztek_prefs.json
.item_finder_prefs.json
.new_tool_prefs.json

# Claude Code local settings/tooling artifacts
.claude/
.understand-anything/
.worktrees/
.superpowers/

# Stray artifact folder (tool path-escaping bug, not project content)
UserskoomoDocumentsCrazyall for cabal/
.cabal_chrome_profile/
.env
*.db
*.sqlite
*.sqlite3
extension/config.local.js
